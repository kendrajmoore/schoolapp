function __convertForIn(obj) {
	return function() {
        var __comp = [];

        Object.keys(obj).forEach(function(k) {
            __comp.push(k);
        });

        return __comp;
    }.call(this);
}